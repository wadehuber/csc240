#ifndef MYFILE_H  /* check if myFile.h has been included already */
#define MYFILE_H  /* it hasn't, so define MYFILE_H */

#define FAVORITE 42

int addone(int n);
void doubleIt(int * n);
int factorial(int n);
void init(void);
#endif
